[project]
name = "london-bikes-dashboard"
version = "0.1.0"
description = "Transport for London Santander Cycles demand exploration and forecasting dashboard"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dash>=2.17.0",
    "plotly>=5.22.0",
    "pandas>=2.2.0",
    "requests>=2.31.0",
    "gunicorn>=22.0.0",
]

[tool.uv]
package = false
